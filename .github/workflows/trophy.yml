name: Trophy card

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Trophy rendering info
        run: echo "Trophy is rendered from trophy-output/trophy.svg in README.md."
